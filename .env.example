# Neon などの PostgreSQL 接続文字列
# 実値は .env に書く（.env は git 管理外）
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require"
# 手元に PostgreSQL が無いときは `npm run db:local` を起動し、次の URL を使う
# DATABASE_URL="postgresql://postgres:password@127.0.0.1:54329/coverage"
